Feedback Fabian:
- upgrades lock/unlock symbol zu aehnlich
- im code editor klarer machen, was line limits sind
- musik funktioniert nicht?
- next upgrade tracking im hud / available upgrades
- victory screen
- integer values merged (255*255) triggern limit
- out of memory / stack overflow bei hoher rekursion
- max instruction limit anheben
- autorun tut nichts ? (ausserdem timer im HUD adden)

- controls im Upgrade tree verbessern; space/enter zum upgrade erlauben und bei single track den trigger auf dem Upgrade statt auf dem Track erlauben
- terminal history (aka press 'up' arrow) auch nach scene wechsel
- bug: upgrades im track zeigen current level / total level an (statt current / max track)
